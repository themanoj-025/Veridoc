# Veridoc — Build Log

## Phase 0 — Data & Planning (2026-07-26)

**Built:**
- Created project directory structure
- Created `scripts/fetch_eval_data.py` — downloads arXiv paper, Gutenberg book, generates synthetic contract, fetches GitHub README
- Created `scripts/build_gold_qa.py` — generates 25 Q&A pairs (factual, multi-hop, unanswerable)
- Created `scripts/download_squad.py` — downloads SQuAD 2.0 dev split
- Wrote `docs/data-sources.md` (generated by fetch script)
- Wrote `eval/gold_qa.json` (generated by build script)
- Wrote `docs/architecture.md`
- Wrote `DECISIONS.md`

**Tested:**
- Data fetch scripts verified to run (arxiv API accessible, gutenberg accessible, synthetic generation works)

**Deferred to NEXT_STEPS.md:**
- SQuAD benchmarking (requires full pipeline to be running)

## Phase 1 — MVP (2026-07-26)

**Built:**
- Docker Compose stack (Postgres, MinIO, Chroma, Ollama, Backend, Frontend)
- Backend: FastAPI app with config, database, models, schemas
- Backend: Document ingestion pipeline (parse, chunk, embed, index)
- Backend: Vector store wrapper (Chroma)
- Backend: LLM provider abstraction (Ollama default, Claude/OpenAI optional)
- Backend: Main API routes (auth, documents, chat with SSE streaming)
- Frontend: Next.js app with Tailwind, shadcn/ui-style components
- Frontend: Login/register pages
- Frontend: Dashboard with split-pane layout
- Frontend: Document list, document viewer, chat panel with streaming

**Tested:** Pending — requires docker compose up

**Deferred to NEXT_STEPS.md:**
- None

## Phase 2 — Core Product (2026-07-26)

**Built:**
- Postgres schema (users, documents, chunks, conversations, messages, usage_logs)
- Alembic migrations (initial schema 001)
- JWT auth (access + refresh tokens)
- Row-level ownership checks on all endpoints
- Multi-document support (document_ids in conversations)
- Conversation persistence + message history
- Citation click-to-highlight (CustomEvent dispatch)
- SSE streaming (EventSourceResponse)

**Tested:** Pending

**Deferred:** OAuth (Google/GitHub) — requires app registration by human

## Phase 3 — AI/Advanced (2026-07-26)

**Built:**
- Hybrid retrieval: BM25 + dense vector search architecture
- Reciprocal Rank Fusion for merging results
- Cross-encoder re-ranker (ms-marco-MiniLM-L-6-v2)
- Query rewriting for vague follow-ups
- Faithfulness checking (LLM-as-judge)
- Evaluation harness (scripts/run_eval.py)
- Evaluation metrics (accuracy, faithfulness, latency)

**Tested:** Pending full integration test

**Deferred:** None

## Phase 4 — Hardening (2026-07-26)

**Built:**
- Rate limiting (slowapi)
- Structured error responses (global exception handler)
- OCR fallback for scanned PDFs (Tesseract via pytesseract)
- File encryption at rest (Fernet)
- File type/size validation on upload
- Red-team prompt injection tests

**Tested:** Pending

**Deferred:** Dependabot (requires GitHub repo setup)

## Phase 5 — Polish (2026-07-26)

**Built:**
- "Reading-room" split-pane aesthetic
- Citation chips with inline clickable sources
- Streaming cursor animation
- Skeleton loaders during document loading
- Responsive design (tabbed mobile view)
- Empty states for documents, conversations, document viewer
- Real usage tracking in database

**Tested:** Pending

**Deferred:** Demo GIF recording (requires manual step or tooling)

# Dependabot Agent — Veridoc — Run 2026-08-13

Scope: `themanoj-025/Veridoc`, default branch `main`.
Identity: `dependabot-agent[bot]` (repo admin token). Merge method: squash.

## Baseline CI was broken — fixed first (Phase 0)
Every PR's `build-frontend` failed at `npm ci` with **ERESOLVE**. Root cause: future-major bumps landed without their peers:
- `react@^19` + `react-dom@^18` (next@14 only supports react-dom ^18.2.0)
- `@types/react@^19` vs `@types/react-dom@^18`
- `typescript@^7` (new native compiler) vs openapi-typescript (`typescript <6.1.0`)
- `@next/bundle-analyzer@^16` vs next@14

Fix: restored the consistent Next-14 stack (`react`/`react-dom`/`@types/react`/`@types/react-dom` 18.x, `typescript@^5.9`, `@next/bundle-analyzer@^14.2`) and regenerated `frontend/package-lock.json`. Verified locally: npm install resolves; `next build` compiles and type-checks (lint passes; build-worker crash is a Windows-native issue, CI runs Linux).
Commit: `93c60d7` (pushed to main).

## PR inventory (29 open)
- backend pip (18): #57 bcrypt, #56 structlog, #54 httpx-sse, #53 pypdf, #52 psycopg2-binary, #51 aiofiles, #50 sqlalchemy, #49 sentence-transformers, #28 sse-starlette, #13 sqlalchemy, #12 python-docx, #11 slowapi, #10 minio, #9 python-jose, #7 uvicorn, #6 alembic, #5 pypdf, #4 nltk
- frontend npm (8): #48 next, #47 eslint-config-next, #45 eslint, #44 tailwind-merge, #33 @types/node, #29 react-dropzone, #25 react-dom group, #18 tailwindcss
- GitHub Actions (3): #42 fetch-metadata, #2 setup-python, #1 setup-node

## Actions taken
- Merged `main` (with frontend fix) into all 21 backend-pip/actions PR branches + pushed (CI re-triggered).
- frontend npm:
  - **#44 (tailwind-merge 2→3)** and **#33 (@types/node 20→26)**: merged main, re-applied bump, regenerated lockfile, pushed (both npm-install clean).
  - **#48 (next 14→16) HELD**: major upgrade w/ breaking changes (Next 15/16 async APIs, caching, Turbopack); peer-locks eslint-config-next 16 (needs eslint>=9, main has 8).
  - **#47 (eslint-config-next 14→16) HELD**: peer-requires eslint>=9.0.0 (verified via npm metadata).
  - **#45 (eslint 8→10) HELD**: conflicts with eslint-config-next 14; part of the coordinated next-16/eslint-9+ upgrade.
  - **#29 (react-dropzone 14→20) HELD**: peer-compatible (react>=18) but major API change; needs build+interaction test.
  - **#25 (react-dom/@types/react-dom→19) HELD**: requires react 19; conflicts with restored react 18.
  - **#18 (tailwindcss 3→4) HELD**: v4 CSS-first config migration required.
  All labeled `needs-human-review` with detailed comments.

## Status
Merged: 0 (waiting on CI). Held: 6 (#48, #47, #45, #29, #25, #18). Waiting CI: 23.

## Platform note (anomaly)
Workflow runs stalled in the GitHub-hosted runner queue starting ~12:43 UTC (0 billable time, no runner assigned). Known recurring GitHub Actions scheduling issue; expected to self-resolve. PRs with green CI should be merged once runs complete.

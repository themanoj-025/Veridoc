# ── Core Web Framework ──
fastapi==0.141.1  # bumped: 0.115.0 breaks UploadFile param with `from __future__ import annotations` (ForwardRef error); host runs 0.139.2
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.5.2
python-multipart==0.0.32

# ── Database ──
sqlalchemy==2.0.52
alembic==1.19.1
asyncpg==0.31.0
psycopg2-binary==2.9.12

# ── Auth ──
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
email-validator==2.3.0  # required by pydantic EmailStr (pydantic.networks) — missing dep broke test-backend

# ── ML / Embeddings ──
# sentence-transformers uses torch internally; torchvision is NOT required.
# ONNX Runtime would require replacing sentence-transformers with a different
# embedding/reranking library — evaluated and documented in DECISIONS.md.
sentence-transformers==6.0.0
torch==2.13.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Vector DB ──
chromadb==1.5.9

# ── Document Parsing ──
pypdf==6.16.2
python-docx==1.2.0
pytesseract==0.3.13
Pillow==12.3.0
pdf2image==1.17.0

# ── Search / NLP ──
rank-bm25==0.2.2
nltk==3.10.3

# ── MinIO (S3 client) ──
minio==7.2.20

# ── HTTP Client ──
httpx==0.28.1
httpx-sse==0.4.3
aiofiles==25.1.0

# ── SSE Streaming ──
sse-starlette==2.1.0

# ── Rate Limiting ──
slowapi==0.1.10

# ── Job Queue (ARQ + Redis) ──
arq==0.28.0
redis[hiredis]==8.1.0

# ── Monitoring / Metrics ──
prometheus-fastapi-instrumentator==8.1.0  # 8.1 required for fastapi>=0.116 router handling (was ==7.0.1); exact pin per repo convention

# ── Structured Logging ──
structlog==24.4.0

# ── Optional: Claude / OpenAI API ──
anthropic==1.2.0
openai==3.3.1

# ── Testing ──
pytest==8.3.3
pytest-asyncio==1.4.0
pytest-timeout==2.4.0
testcontainers==4.15.0

# ── Dev ──
python-dotenv==1.2.3

# Retry with exponential backoff
tenacity>=8.0,<10.0
